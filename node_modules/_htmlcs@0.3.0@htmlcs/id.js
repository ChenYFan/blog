var map = require('./lib/default/rule-map');

var ids = [];
Object.keys(map).forEach(function (key) {
    map[key].split(/\s*,\s*/).forEach(function (n) {
        ids.push(parseInt(n, 10));
    });
});

var max = Math.max.apply(Math, ids);

console.log('Max ID: %s', max);
