#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_supports-color@1.3.1@supports-color/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_supports-color@1.3.1@supports-color/cli.js" "$@"
  ret=$?
fi
exit $ret
