#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../write-good/bin/write-good.js" "$@"
  ret=$?
else 
  node  "$basedir/../write-good/bin/write-good.js" "$@"
  ret=$?
fi
exit $ret
